# NeoDomain Research Notes

The public page `https://neodomain.cn/video-works-view?id=211` is accessible and returns public showcase metadata for `《万物生：问心》`.

Observed public context:

- The showcase title is `《万物生：问心》`.
- The public presentation shows a node-based production canvas with repeated creature, crowd, character, and scene iterations.

Safe extraction limitation:

- Public access does not expose the complete underlying prompt-node text.
- No cookies, local storage, authentication tokens, or private canvas data were collected.

Use this note only as context. Do not claim that prompt templates were extracted unless the user later provides an export, share link, or copied node data.
